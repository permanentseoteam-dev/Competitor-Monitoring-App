# Legacy (do not use)

Archived leftovers from the old Next.js app and a nested duplicate project.

| Path | What it is |
|------|------------|
| `nextjs-root/` | Root Next config (`next.config.ts`, ESLint, PostCSS, etc.) |
| `nextjs-src/` | Old App Router / React components and `.ts` duplicates |
| `competitor-monitor-app/` | Snapshot of the nested `competitor-monitor/` Next app |

The live Express app is at the repo root (`server.js`, `src/`, `views/`, `public/`).
