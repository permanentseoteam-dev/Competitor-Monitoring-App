# Deprecated Next.js app

This folder was a leftover nested Next.js project after the Express migration.
Source snapshot lives in `legacy/competitor-monitor-app`.
Safe to delete the root `competitor-monitor` folder (including its node_modules) when nothing has it locked.
